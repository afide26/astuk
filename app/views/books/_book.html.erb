
<div class="col-md-3">
	<div class = "thumbnail">
		  <%= link_to book_path(book) do %>
		  <%= image_tag 'book.jpg', class: 'img img-responsive' %>
		  <%end%>
		  <div class="caption text-right">
		    <p><strong><%= link_to book.name, book_path(book) %></strong></p>
		    <p>Price: $<%= book.price.to_f/100%>0</p>
		  </div>
	</div>
</div>
