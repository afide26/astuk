<p id="notice"><%= notice %></p>

<div class="row">
  <div class="col-md-6">
    <%= image_tag 'book.jpg', class: 'img img-responsive' %>
  </div>
  <div class="col-md-6">
    <h3 class="alert alert-info text-center"><%= @book.name %>by:<%= @book.author %></h3>
    <blockquote>
      <p><%= @book.description %></p>
      <footer>
        <cite>Pragmatic Books</cite>
      </footer>
    </blockquote>
    <h4 class="text-right">$<%= @book.price.to_f/100 %>0</h4>
  </div>
</div>

<%= link_to 'Edit', edit_book_path(@book) %> |
<%= link_to 'Back', books_path %>